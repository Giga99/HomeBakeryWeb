class Comment {
    constructor(text, username, date) {
        this.text = text;
        this.username = username;
        this.date = date; // JavaScript equivalent of Instant.now().minus would be needed
    }
}
